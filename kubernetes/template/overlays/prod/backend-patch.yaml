apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: alpine
        resources:
          limits:
            memory: "512Mi"
            cpu: "1000m"
